# MirrorCast V0.3 设计方案（低延迟 · 硬件编码 · 收发同款）

> 状态：**设计稿，未动代码**。基于 MirrorCast V0.2 代码副本演进（V0.3 文件夹已建）。
> 决策记录：接收端 = 另一台安卓设备（同一 APK 双角色）；自匹配 = 二维码握手 + 记住设备；默认规格 = 1080p/30fps/CBR 10Mbps；当前阶段 = 先出设计文档评审（4C）。

---

## 1. 目标与非目标

### 1.1 目标
| # | 目标 | 验收线 |
|---|------|--------|
| G1 | 端到端低延迟 | 稳态实测 100~150ms（含 16~32ms 采集地板） |
| G2 | 硬件编码 + 接收端 APP 渲染 | MediaCodec 编/解，无浏览器依赖 |
| G3 | 双通道架构 | 独立控制通道(8090) + 视频通道(8091) |
| G4 | 平滑连续推送 | 包间隔 ≤2.4ms，无突发（30fps ≈ 416 包/秒 ≈ 每帧 ~14 片） |
| G5 | 无音频 | 视频流无音频通道 |
| G6 | 局域网直连 | 手机→手机，不绕公网 |
| G7 | 收发同款 + 自匹配 | 单 APK 双角色；扫码配对 + 记住设备下次同网自动连 |
| G8 | 保留 V0.2 已验证的稳定性资产 | 授权流程/前台服务/唤醒锁/诊断闭环 |

### 1.2 非目标（本版不做，架构预留）
- 公网穿透、音频推流、多接收端大规模分发（协议预留即可）
- 熄屏继续投屏 / 远程控制：**可选增强**，见 §7，不进核心里程碑
- 浏览器观看端：H.264 无法 `<img>` 直显，V0.2 的 viewer.html 仅保留为调试辅助（不承诺维护）

---

## 2. 总体架构

```
┌──────────── 发送端（角色A：vivo 相机前台）────────────┐
│  MediaProjection → VirtualDisplay(1080p)              │
│      → GLES 翻转 → 编码器输入 Surface                  │
│      → H264Encoder (H.264, 1080p30, CBR 10Mbps)       │
│      → NAL 分片(1~2KB) → 平滑推送器(2.4ms 节奏)        │
│      → TCP :8091 视频通道                             │
│  控制服务 :8090（JSON）←→ 指令/心跳/配对/统计            │
└────────────────────────┬──────────────────────────────┘
                         │ Wi-Fi 局域网（TCP）
┌────────────────────────┴──────────────────────────────┐
│ 接收端（角色B：另一台安卓，同一 APK）                    │
│  TCP :8091 收流 → 接收缓冲(只留最新帧) → MediaCodec 硬解 │
│  → SurfaceView 显示（镜像/适应/全屏）                    │
│  控制服务 :8090 → 发指令（翻转/画质/停止）、收统计        │
└──────────────────────────────────────────────────────┘
```

**角色协商**：启动后用户选"发起投屏"或"接收投屏"；扫码配对成功的双方自动确定角色（扫二维码方=接收端，被扫方=发送端），并记忆 peer 记录。

---

## 3. 模块设计（对照 V0.2 文件映射）

| V0.3 模块 | 来源 | 职责 | 变更量 |
|---|---|---|---|
| `CastService.kt` | 复用 V0.2 | 授权/前台服务/唤醒锁/生命周期；改为装配 H264Encoder + 发送器 | 中改 |
| `H264Encoder.kt` | 复用 V0.2 骨架 | MediaCodec Surface 硬编；CBR + KEY_LATENCY + 无B帧 + I帧 1s；输出 Annex-B | 小改 |
| ~~GlesFlipper.kt~~ | **已取消（设计变更）** | 翻转移至接收端渲染层（TextureView scaleX=-1，GPU 变换零成本）；发送端 VirtualDisplay 直连编码器输入 Surface，零拷贝零翻转，CPU≈0（腾给相机成像算法） | — |
| `SoftwareFlipFallback.kt` | 新增（备选） | ImageReader → RGBA → 软件行交换 → queueInputBuffer 喂编码器 | 中（Phase 0 先验证管线用） |
| `NalSlicer.kt` | 新增 | 将每帧 Annex-B 切成 ~1~2KB 小片 + 帧头（seq/时间戳/类型） | 小 |
| `PacedPusher.kt` | 新增 | 定时线程按 ≤2.4ms 间隔推进小片写入（LockSupport.parkNanos） | 中 |
| `VideoServer.kt` | 替代 StreamServer | TCP:8091 视频通道（每连接一线程），TCP_NODELAY + 分片推送 | 中 |
| `ControlServer.kt` | 替代 StreamServer(控制部分) | TCP:8090 JSON 指令/应答/心跳/统计 | 中 |
| `ReceiverEngine.kt` | **新增** | 收流 → 解码 → 呈现；核心策略：**只渲染最新帧，零积压**（照搬 V0.2 丢旧帧保实时哲学） | 大 |
| `SelfMatch.kt` | 新增 | 二维码握手（复用 QrCode.kt）+ peer 记忆存储 + 同网自动重连 | 中 |
| `NetInfo.kt` / `DiagLog.kt` / `QrCode.kt` | 复用 V0.2 | 不变 | 零 |
| `StreamServer.kt` / `JpegCompressor.kt` / `FrameFlipper.kt` / `H264Muxer.kt` | 搁置 | MJPEG 路径退役；FrameFlipper 算法知识迁移进 SoftwareFlipFallback | — |

**依赖**：仅 Android 平台 API（MediaCodec/MediaProjection/NSD 可选），无第三方库新增；保持项目"自研轻量"风格。

---

## 4. 协议设计

### 4.1 自匹配（二维码握手）
- 发送端显示二维码，内容 JSON：`{ver, role:"source", ip, vport:8091, cport:8090, deviceId, name}`
- 接收端扫码 → 向 `cport` 发 `PAIR {deviceId, name, ip}` → 发送端回 `PAIR_ACK {sessionId, params{width,height,fps,bitrate,flip}}`
- 双方写入本地 peer 表（deviceId → ip/port/name），下次同网启动时自动尝试重连（心跳保活，断线指数退避后重试）
- 失败兜底：手动输 IP（输入框 + 历史记录）

### 4.2 控制通道（TCP:8090，JSON，4 字节长度前缀）
- 接收端→发送端：`PAIR` / `UNPAIR` / `SET_FLIP(0|1)` / `SET_QUALITY(档)` / `SET_FPS` / `SET_CROP` / `SET_OFFSET` / `STOP` / `PING`
- 发送端→接收端：`PAIR_ACK` / `SESSION_START` / `STATS`(2s: fps/kbps/pipeMs/CPU/温度) / `ERROR`

### 4.3 视频通道（TCP:8091）
- 每帧封装：帧头（JSON：seq、发帧时刻 ms、类型 I/P、NAL 片数 N）+ N 个 NAL 小片
- **SPS/PPS**：连接建立时先发一次；每个 IDR 帧附带再发一次（重连/丢包后秒恢复）
- **平滑推送**：`PacedPusher` 按目标码率计算片大小与间隔——10Mbps / 416pps ≈ 3KB/片，保守取 2KB/片；每帧 33.3ms 内 ~14 片按 2.4ms 节奏送出；`TCP_NODELAY` 打开
- **接收端零积压**：只保留完整最新帧；解码采用"渲染最新解码帧，丢弃积压"策略（等效 V0.2 队列=1）

### 4.4 传输选型决策（已定稿）
- **视频通道：UDP + RTP**（复用 huihuan 原型的 RtpPacket/SeqUtil，包名迁入 com.mirrorcast.v03.cast.rtp）。理由：接受丢帧/乱序 → 无需 TCP 的可靠性与队头阻塞；RTP 语义天然适配"2.4ms 节奏推送"与丢旧帧保实时；局域网余量大。乱序由接收端小窗口排序（seq 回绕安全），缺帧丢整帧，画面在下一个 IDR（≤1s）恢复；NACK 重传仅预留字段，v1 不做。
- **控制通道：TCP**（JSON，4 字节长度前缀），可靠性要求高、流量小。
- 重传语义：关键帧不重传（接受 1s 内自愈），后续可加"仅关键帧 NACK"。

---

## 5. 延迟账本（设计目标）

| 环节 | 量级 | 备注 |
|---|---|---|
| 屏幕合采集地板 | 16~32ms | 物理底线，任何方案不变 |
| 硬编（Surface 输入零拷贝） | 3~8ms | CBR 低延迟配置 |
| 局域网传输（帧体量缩小 10~50 倍） | 2~10ms | 10Mbps 预算 vs 5G Wi-Fi 实测余量（V0.2 心跳日志） |
| 硬解 | 3~8ms | MediaCodec 硬件解码 |
| 显示合成 | ~16ms | SurfaceView 直出 |
| 调度/缓冲余量（保守） | 10~30ms | 含 Wi-Fi 抖动、Android 调度 |
| **合计理论 / 实测预期** | **80~120ms / 100~150ms** | 对比 V0.2 实测 300~500ms |

> 平滑推送不贡献下限，贡献稳定性：抗 AP 队列 bufferbloat，消除 V0.2 偶发延迟尖峰。

---

## 6. 关键实现要点与备选

1. **发送端零拷贝 + 接收端翻转（设计变更，2026-08 拍板）**：不再做任何像素处理——VirtualDisplay 直接输出到编码器输入 Surface（无 ImageReader 回读、无翻转、无 Canvas）；镜像改由接收端完成（TextureView scaleX=-1 或后续 GLES 着色器），翻转开关天然成为接收端功能。收益：发送端视频路径 CPU 约 0，把 CPU/GPU 预算全部让给同机运行的相机 App（成像算法），降低发热干扰。
2. **接收端零积压**：先直接 MediaCodec Surface 模式（timestamps 连续）；若实测观察到解码缓冲增长，切换为"解码到 buffer、只提交最新帧到呈现"模式。
3. **定时器精度**：Android 调度抖动 1~3ms；2.4ms 规格在余量网络下宽裕，若实测超差可降档到 3ms 间隔（规格由常量控制，留调参口）。
4. **Android 14/16 限制**：完整沿用 V0.2 已趟平的流程（先 startForeground(mediaProjection) 再 getMediaProjection；Android 16 先注册回调再 createVirtualDisplay）。
5. **编码参数**：BASELINE 无 B 帧；`KEY_I_FRAME_INTERVAL=1`；`KEY_LATENCY=1`（API 30+）；CBR；1080p30 / 10Mbps 默认，提供 720p30/6Mbps 与 1080p60/15Mbps 档位（60fps 档标注发热风险）。
6. **统计与诊断**：复用 DiagLog；发送端心跳 10s 记录 fps/kbps/温度/pipeMs（沿用 V0.2 差分口径）；接收端显示实时统计。
7. **保活**：发送端 PARTIAL_WAKE_LOCK（复用 V0.2）；接收端保屏常亮提示。

---

## 7. 可选增强：熄屏继续投屏 + 远程控制（对应提问，本次仅设计不实现）

### 7.1 机制澄清（已在评审中说明）
- V0.2 即 VirtualDisplay 管线；VirtualDisplay 只产**原始像素**，编码不可省（1080p30 原始 RGBA ≈ 2Gbps，无法网络传输）；V0.3 能省的是回读/拷贝/软件翻转（Surface 输入零拷贝已入主方案）。
- 真锁屏/doze 下 MediaProjection 采集冻结（V0.2 R-7 实锤）。

### 7.2 第三方 APP 近似"熄屏保活"方案
- `PARTIAL_WAKE_LOCK`（CPU 不睡）＋ 全黑全屏 overlay ＋ `Settings.System.SCREEN_BRIGHTNESS=0` → 背光熄灭但系统不入睡，GPU/SurfaceFlinger 继续合成。
- **需真机验证（30 分钟实验，并入 Phase 0）**：黑屏期间发送端心跳日志帧率是否保持非零。
- 风险：ROM 差异（部分 vivo 版本限制亮度写入/后台黑屏 overlay）；GPU 仍耗电。

### 7.3 远程控制
- 唯一现实路径 = 无障碍服务（`GLOBAL_ACTION_WAKEUP` + 手势/点击注入；V0.2 C4 已有结论）。
- 建议范围：先只做"唤醒 + 点按"（对应拍照/切换镜头场景），vivo 相机 UI 元素匹配按诊断日志迭代。
- 落地时机：**V0.3 里程碑之后**（可作 V0.4），不进本版验收。

---

## 8. 里程碑与验收

| 阶段 | 内容 | 验收 |
|---|---|---|
| **Phase 0（回环验证）** ~1~2 天 | 同机：本机硬编→硬解→小窗显示（先用 SoftwareFlipFallback 跑通）；附加黑屏保活实验 | 回环画面正常；秒表实测回环延迟；黑屏实验结论 |
| **Phase 1（传输）** ~2~3 天 | 双通道 TCP + NAL 分片 + 2.4ms 平滑推送 + 控制协议 | 两机（或手机↔PC 工具）联调 1080p30 稳定出图；稳态延迟实测 <150ms 记录 |
| **Phase 2（双角色 APP）** ~2~4 天 | 收发同款 UI + 二维码自匹配 + 记忆重连 + 控制项（翻转/画质/统计） | 全流程可用：扫码即连、双机互投、断线自动恢复 |
| **Phase 3（打磨）** 按需 | 重连退避、稳定性、可选 GLES 主方案替换、可选远程控制 | 长期稳定运行 |

预估：Phase 2 到可用版本 **5~9 个工作日**。

---

## 9. 风险清单

| # | 风险 | 等级 | 缓解 |
|---|------|------|------|
| R-1 | GLES 翻转进编码器 Surface（核心新工程） | 中 | Phase 0 先用软件备选跑通全链路，再并行攻关 GLES |
| R-2 | 接收端解码积压导致延迟漂移 | 中 | 只渲染最新帧策略；Phase 1 加延迟监控（收包时间戳 vs 呈现） |
| R-3 | 定时器抖动破坏 <2.4ms 规格 | 低-中 | 余量网络兜底；间隔常量可调；必要时降 3ms |
| R-4 | 熄屏保活因 ROM 差异失败 | 中 | 不阻塞核心周期；Phase 0 实验后单独决定 |
| R-5 | 不同机型编/解码能力差异（H.264 Baseline 全兼容） | 低 | 连接时能力协商（回传支持分辨率），降档兜底 |
| R-6 | 系统省电杀后台 / Android 授权变化 | 低 | 完整复用 V0.2 已验证方案（前台服务/白名单引导/回调注册） |
| R-7 | 两设备时钟不同步影响延迟统计 | 低 | 统计换算为"单端测量"（同端计算发送时刻与到达时刻差：RTT/管道） |

---

## 10. 评审结论（已定稿）

| # | 问题 | 结论 |
|---|------|------|
| 1 | 熄屏保活实验 | ✅ 接受：Phase 0 先实验、结论再定是否纳入本版 |
| 2 | 远程控制（无障碍） | ✅ 放 V0.4，不进 V0.3 |
| 3 | 画质档位 | ✅ 做三档：1080p30/10Mbps（默认）、720p30/6Mbps、1080p60/15Mbps |
| 4 | 多接收端 | ✅ 协议预留，不做 UI |

**版本共存要求（新）**：V0.3 与 V0.2 在手机上并存、互不取代。V0.3 将改 `applicationId = "com.mirrorcast.v03"`、label「镜像投屏V0.3」、versionName 0.3.0，与 V0.2（com.mirrorcast）同时安装。

**待定项（并入 Phase 0/1 决策）**：工作区已有用户的回环原型 `huihuan/`（包 com.lowlatency.cast）：
- 已有：`RtpPacket`（标准 RTP 12B 头 + flags：帧首/关键帧/FU-A 分片/帧尾/重传标记）、`SeqUtil`（回绕安全序号）、`CastService`（token→CastBus）。
- 缺：`CastSession`、`LatencyStats`、编码、解码、渲染（仅有引用）。
- 该原型表明传输方向为 **UDP + RTP（含 NACK 重传语义）**，与 §4.4 的"v1 用 TCP"冲突 → 待用户确认后修订 §4.4。